// Content script — kept minimal. The heavy lifting is done by the injected
// function in popup.js via chrome.scripting.executeScript. This file exists
// so the extension is declared as a content script (required for scripting
// permission to work on the page).
